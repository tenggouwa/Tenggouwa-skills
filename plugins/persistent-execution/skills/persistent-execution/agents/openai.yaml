interface:
  display_name: "Persistent Execution (Goal mode)"
  short_description: "Use with /goal for long tasks that must not stop"
  default_prompt: "/goal Use $persistent-execution to carry this task through to a verified, safe completion."
